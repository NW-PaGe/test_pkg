[project]
name = "test-pkg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "polars>=1.32.3",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
